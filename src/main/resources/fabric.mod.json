{
	"schemaVersion": 1,
	"id": "${id}",
	"version": "${version}",
	"name": "${name}",
	"description": "${description}",
	"authors": [
		"${author}"
	],
	"contact": {
		"homepage": "https://github.com/Ayydxn/Voxellight",
		"sources": "https://github.com/Ayydxn/Voxellight/issues"
	},
	"license": "${license}",
	"icon": "assets/${id}/${icon_file}",
	"environment": "*",
	"entrypoints": {
		"preLaunch": [
			"com.ayydxn.voxellight.VoxelleightPreLaunchEntrypoint"
		],
		"client": [
			"com.ayydxn.voxellight.VoxellightClientMod"
		]
	},
	"mixins": [
		"${id}.mixins.json"
	],
	"depends": {
		"minecraft": "~${minecraft_version}",
		"fabricloader": ">=${fabric_loader_version}",
		"fabric-api": "${fabric_api_version}"
	}
}